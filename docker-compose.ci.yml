version: "3.8"

services:
  ollama:
    # CI runners have no NVIDIA runtime; keep it simple.
    deploy: {}

  nginx:
    # Avoid binding privileged ports on shared runners (optional)
    ports:
      - "8080:80"
